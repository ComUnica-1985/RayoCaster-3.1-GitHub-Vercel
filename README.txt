RayoCaster 4.0.2 - parche de acceso web

Copie el contenido de esta carpeta sobre la raiz del repositorio web, conservando .git, .env y deployment-secrets.txt.
Luego ejecute npm ci, npm run verify, npm audit --omit=dev, git add -A, commit y push.
No requiere cambiar el EXE 4.0.1.
